/* AUTOMATICALLY GENERATED VERILOG-2001 SOURCE CODE.
** GENERATED BY CLASH 0.99.3. DO NOT MODIFY.
*/
module TestMachine_stimuliGenerator
    ( // Inputs
      input  clk // clock
    , input  rst // asynchronous reset: active high

      // Outputs
    , output wire [7:0] result 
    );
  reg [2:0] \#tup_app_arg ;
  reg [2:0] s;
  wire signed [63:0] wild;
  wire signed [63:0] \#wild_app_arg ;
  wire  \#tup_app_arg_selection_res ;
  wire [55:0] \#vecflat ;

  assign \#tup_app_arg_selection_res  = s < 3'd6;

  always @(*) begin
    if(\#tup_app_arg_selection_res )
      \#tup_app_arg  = s + 3'd1;
    else
      \#tup_app_arg  = s;
  end

  assign \#vecflat  = {8'b00001011
                      ,8'b00010100
                      ,8'b00011110
                      ,8'b00001010
                      ,8'b11010010
                      ,8'b00010101
                      ,8'b00010111};

  // indexVec begin
  wire [7:0] vec [0:7-1];

  genvar i;
  generate
  for (i=0; i < 7; i=i+1) begin : mk_array
    assign vec[(7-1)-i] = \#vecflat [i*8+:8];
  end
  endgenerate

  assign result = vec[(wild)];
  // indexVec end

  // register begin
  always @(posedge clk or posedge rst) begin : TestMachine_stimuliGenerator_register
    if (rst) begin
      s <= 3'd0;
    end else begin
      s <= \#tup_app_arg ;
    end
  end
  // register end

  assign wild = $signed(\#wild_app_arg );

  assign \#wild_app_arg  = $unsigned(s);
endmodule

